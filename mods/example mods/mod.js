// mods/example-mod/mod.js
document.addEventListener('DOMContentLoaded', () => {
    const newElement = document.createElement('p');
    newElement.textContent = 'This is an example mod!';
    document.body.appendChild(newElement);
});
